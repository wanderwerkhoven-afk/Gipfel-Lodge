<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Exclude existing files and directories
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Rewrite all other routes to the SPA index.html
  RewriteRule ^ index.html [L]
</IfModule>
