const fs = require('fs'); const jsdom = require('jsdom'); const { JSDOM } = jsdom; const html = fs.readFileSync('index.html', 'utf8'); const dom = new JSDOM(html); const doc = dom.window.document; doc.querySelectorAll('script[src]').forEach(script => { const src = script.getAttribute('src'); if (src && (src.includes('router.js') || src.includes('i18n_') || src.includes('i18n.js'))) { script.remove(); } }); console.log('Scripts left:', doc.querySelectorAll('script[src]').length); 
