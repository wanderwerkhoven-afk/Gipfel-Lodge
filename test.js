const fs = require('fs'); const jsdom = require('jsdom'); const { JSDOM } = jsdom; const html = fs.readFileSync('index.html', 'utf8'); const dom = new JSDOM(html); const doc = dom.window.document; const el = doc.querySelector('[data-i18n=\"maint-title\"]'); console.log('Original HTML:', el.outerHTML); const textOverrides = { nl: { 'maint-title': 'GEWIJZIGDE TITEL UIT FIRESTORE' } }; const lang = 'nl'; const key = el.getAttribute('data-i18n'); const text = textOverrides[lang][key]; el.innerHTML = text; console.log('Modified HTML:', el.outerHTML);
